-r requirements.txt

# Starlette's current TestClient prefers httpx2 and warns when it falls back
# to legacy httpx. This is test-only; Athena's runtime does not need it.
httpx2==2.12.0
pyflakes==3.4.0
